{
  "name": "auction-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo dev",
    "dev:user": "turbo dev --filter=@auction/user",
    "dev:admin": "turbo dev --filter=@auction/admin",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "dependencies": {
    "jose": "^6.1.3"
  }
}
